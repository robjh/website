			<tr>
				<td><img src="${icon_url}" alt="[${icon_alt}]" /></td>
				<td><a href="${href}" data-path="${data_path}" data-type="${data_type}" data-mime="${mime}">${text}</a></td>
				<td>${mime}</td>
			</tr>
